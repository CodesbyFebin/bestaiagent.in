import type { Metadata } from "next";import Link from "next/link";export const metadata:Metadata={title:"AI Agent Research",description:"Research methodology and preserved reports with fail-closed indexing.",alternates:{canonical:"/research"}};const reports=[["state-of-ai-agents-india-2026","State of AI agents in India 2026"],["voice-latency-report","Voice latency report"]] as const;export default function Page(){return <div className="shell detail"><p className="eyebrow">Research</p><h1 style={{fontSize:"48px"}}>Reproducible research</h1><p className="lead">Research pages require methods, source data and raw outputs before benchmark conclusions are indexed.</p><div className="grid">{reports.map(([slug,title])=><article className="card" key={slug}><p className="eyebrow">refreshing · noindex</p><h2><Link href={`/research/${slug}`}>{title}</Link></h2><p>Legacy route preserved while the evidence bundle is rebuilt.</p></article>)}</div></div>}
