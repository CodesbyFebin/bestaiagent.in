import { SITE } from "@/lib/site";export const dynamic="force-static";export async function GET(){const text=`# BestAIAgent.in\n\nEvidence-first authority graph for AI agents, models, frameworks, providers and MCP infrastructure.\n\n## Core\n- ${SITE.url}/agents\n- ${SITE.url}/models\n- ${SITE.url}/frameworks\n- ${SITE.url}/providers\n- ${SITE.url}/compare\n- ${SITE.url}/india\n- ${SITE.url}/mcp\n- ${SITE.url}/methodology\n\nMachine catalog: ${SITE.url}/catalog.json\n`;return new Response(text,{headers:{"Content-Type":"text/plain; charset=utf-8"}})}
