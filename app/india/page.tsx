import type { Metadata } from "next";import Link from "next/link";import { entitiesByType } from "@/lib/catalog";export const metadata:Metadata={title:"India & Indic AI",description:"Indian and Indic AI models, local inference and agent infrastructure with primary-source evidence.",alternates:{canonical:"/india"}};export default function Page(){const models=entitiesByType("model").filter((m)=>Boolean(m.indiaRelevance));return <div className="shell detail"><p className="eyebrow">India AI authority hub</p><h1 style={{fontSize:"48px"}}>Indian & Indic AI</h1><p className="lead">A source-first map of Indian and Indic model work. India relevance is not treated as a compliance or sovereignty guarantee.</p><section className="prose"><h2>Verified model identities</h2><ul>{models.map((m)=><li key={m.id}><Link href={`/models/${m.slug}`}>{m.name}</Link> — {m.indiaRelevance}</li>)}</ul><h2>Local inference</h2><p><Link href="/local-llm-benchmarks-india">Read the reproducible local-LLM benchmark methodology.</Link></p></section></div>}
