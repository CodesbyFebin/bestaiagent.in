import type { Metadata } from "next";import Link from "next/link";import { mcpClusters,mcpServers } from "@/lib/mcp";export const metadata:Metadata={title:"Model Context Protocol (MCP) Authority Hub",description:"MCP servers, agent frameworks and local-LLM integration organized by source provenance.",alternates:{canonical:"/mcp"}};export default function Page(){return <div className="shell detail"><p className="eyebrow">MCP authority hub</p><h1 style={{fontSize:"48px"}}>Model Context Protocol</h1><p className="lead">Source-linked MCP infrastructure without invented compliance, adoption or performance claims.</p><section className="prose"><h2>Knowledge clusters</h2>{mcpClusters.map(([name,description])=><div key={name}><h3>{name}</h3><p>{description}</p></div>)}</section><section><h2>MCP server routes</h2><div className="grid">{mcpServers.map((server)=><article className="card" key={server.slug}><p className="eyebrow">{server.status}</p><h3><Link href={`/mcp/servers/${server.slug}`}>{server.name}</Link></h3><p>{server.note}</p></article>)}</div></section></div>}
