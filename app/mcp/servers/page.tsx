import type { Metadata } from "next";import Link from "next/link";import { mcpServers } from "@/lib/mcp";export const metadata:Metadata={title:"MCP Servers Directory",description:"Source-linked MCP server directory with explicit verification status.",alternates:{canonical:"/mcp/servers"}};export default function Page(){return <div className="shell detail"><h1 style={{fontSize:"48px"}}>MCP servers</h1><p className="lead">Legacy routes are retained, while publication status follows canonical-source verification.</p><div className="grid">{mcpServers.map((server)=><article className="card" key={server.slug}><p className="eyebrow">{server.status}</p><h2><Link href={`/mcp/servers/${server.slug}`}>{server.name}</Link></h2><p>{server.note}</p></article>)}</div></div>}
