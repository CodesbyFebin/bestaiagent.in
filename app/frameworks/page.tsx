import type { Metadata } from "next";import { EntityDirectory } from "@/components/EntityDirectory";export const metadata:Metadata={title:"Agent Frameworks",description:"Evidence-backed AI agent frameworks and orchestration systems.",alternates:{canonical:"/frameworks"}};export default function Page(){return <EntityDirectory type="framework" title="Agent frameworks" description="Framework identities backed by primary source evidence, without synthetic rankings."/>}
