import type { Metadata } from "next";import Link from "next/link";import { categories } from "@/lib/legacy";export const metadata:Metadata={title:"AI Agent Categories",description:"Task-oriented AI agent category hubs with evidence-first publication rules.",alternates:{canonical:"/categories"}};export default function Page(){return <div className="shell detail"><p className="eyebrow">Browse by task</p><h1 style={{fontSize:"48px"}}>AI agent categories</h1><p className="lead">Category pages organize canonical entities; they do not create duplicate entity URLs.</p><div className="grid">{categories.map(([slug,title,description])=><article className="card" key={slug}><h2><Link href={`/categories/${slug}`}>{title}</Link></h2><p>{description}</p></article>)}</div></div>}
