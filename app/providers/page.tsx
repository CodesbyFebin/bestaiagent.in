import type { Metadata } from "next";import { EntityDirectory } from "@/components/EntityDirectory";export const metadata:Metadata={title:"AI Providers",description:"Provider identities and source evidence for the BestAIAgent.in entity graph.",alternates:{canonical:"/providers"}};export default function Page(){return <EntityDirectory type="provider" title="AI providers" description="Provider records are published only when the identity gate passes. Product claims require separate evidence."/>}
