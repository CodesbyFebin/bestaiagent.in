import { publicEntities } from "@/lib/catalog";import { SITE } from "@/lib/site";export const dynamic="force-static";export async function GET(){const lines=["# BestAIAgent.in — full public entity index","","Only entities that pass the primary-evidence publication gate appear below.","",...publicEntities.map((e)=>`- ${e.name} (${e.type}) — ${SITE.url}/${e.type}s/${e.slug} — source: ${e.sourceUrl}`)];return new Response(lines.join("\n"),{headers:{"Content-Type":"text/plain; charset=utf-8"}})}
