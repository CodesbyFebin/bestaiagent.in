import type { Metadata } from "next";import Link from "next/link";import { comparisons } from "@/lib/comparisons";export const metadata:Metadata={title:"Compare AI Agents & Frameworks",description:"Evidence-aware comparisons with legacy routes preserved and unsupported winner claims removed.",alternates:{canonical:"/compare"}};export default function Page(){return <div className="shell detail"><p className="eyebrow">Comparison graph</p><h1 style={{fontSize:"48px"}}>Compare AI systems</h1><p className="lead">A comparison is indexable only when both entities and the compared fields have enough evidence. Legacy comparison URLs remain available while evidence is refreshed.</p><div className="grid">{comparisons.map((item)=><article className="card" key={item.slug}><p className="eyebrow">{item.status}</p><h2><Link href={`/compare/${item.slug}`}>{item.title}</Link></h2><p>{item.notes[0]}</p></article>)}</div></div>}
