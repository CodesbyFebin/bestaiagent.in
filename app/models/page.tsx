import type { Metadata } from "next";import { EntityDirectory } from "@/components/EntityDirectory";export const metadata:Metadata={title:"AI Models Directory",description:"Evidence-backed AI model cards with India and local-model coverage.",alternates:{canonical:"/models"}};export default function Page(){return <EntityDirectory type="model" title="AI models" description="Verified model identities and model-card facts, with unknown fields left unknown."/>}
